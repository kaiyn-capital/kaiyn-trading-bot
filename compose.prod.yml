services:
  bot:
    image: ${BOT_IMAGE:?Set BOT_IMAGE to a GHCR image tag or digest}
    build: !reset null

  maintenance:
    image: ${BOT_IMAGE:?Set BOT_IMAGE to a GHCR image tag or digest}
    build: !reset null
